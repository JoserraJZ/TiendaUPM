<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <property name="hibernate.connection.driver_class">org.sqlite.JDBC</property>
        <property name="hibernate.connection.url">jdbc:sqlite:tienda.db</property>
        <property name="hibernate.dialect">org.hibernate.community.dialect.SQLiteDialect</property>

        <!-- SOLO create -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <property name="show_sql">true</property>

        <!-- Tus entidades -->
        <mapping class="upm.products.Product"/>
        <mapping class="upm.Cashier"/>
        <mapping class="upm.Client"/>
        <mapping class="upm.Service"/>
        <mapping class="upm.Ticket"/>

    </session-factory>
</hibernate-configuration>
